import { User } from "./interfaces/User";

let a: User = {uuid: 224, name: "Bobby", age: 24, ans: 12, location: "pune" };

console.log(`User is ${a.name}, age is ${a.age}, location is ${a.location}`);